export interface IPracticaSearch {
    // type: string; // 'simplequery' | 'multimatch' | 'suggest';
    cadenaInput?: string;
    codigo?: string;
    // nombre?: string;
    // descripcion?: string;
}
